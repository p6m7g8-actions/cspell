name: "CSpell"

on:
  pull_request: {}

permissions:
  contents: read

jobs:
  cspell:
    name: CSpell
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: p6m7g8-actions/checkout@main

      - name: Run cspell
        uses: ./
